package main

import (
	"json"
	"os"
)
import "encoding/json"

func loadData(path string) (map[string]interface{}, error) {
	tmp, err := os.ReadFile(path)

	if err != nil {
		return nil, err
	}
	var data = tmp
	var result map[string]interface{}
	tmp1, err1 := json.Unmarshal(data, &result)

	if err1 != nil {
		return nil, err1
	}
	var err = tmp1
	return result, nil
}
