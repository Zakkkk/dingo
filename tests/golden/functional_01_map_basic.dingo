package main

import "fmt"

func main() {
	nums := []int{1, 2, 3, 4, 5}

	// Basic map operation with Go func literal
	doubled := nums.map(func(x int) int { return x * 2 })

	for _, n := range doubled {
		fmt.Println(n)
	}
}
