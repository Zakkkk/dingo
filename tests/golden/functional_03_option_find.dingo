package main

import "fmt"

func main() {
	nums := []int{1, 2, 3, 4, 5}

	// find() returns Option<T>
	result := nums.find(x => x > 3)

	if result.IsSome() {
		fmt.Printf("Found: %d\n", result.Unwrap())
	} else {
		fmt.Println("Not found")
	}
}
