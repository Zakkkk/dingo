package main

import (
	"os"
)

func readConfig(filePath2 string) ([]byte, error) {
	tmp, err := os.ReadFile(filePath)

	if err != nil {
		return nil, err
	}
	var data = tmp
	return data, nil
}
func test() {
	tmp, err := readConfig("config.yaml")

	if err != nil {
		return nil, err
	}
	var a = tmp
	println(string(a))
}
