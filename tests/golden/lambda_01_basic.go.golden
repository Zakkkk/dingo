package main

func main() {
	// Rust-style lambda syntax with explicit types
	add := func(a int, b int) int { a + b }
	double := func(x int) int { x * 2 }
	greet := func(name string) string { "Hello, " + name }
}
