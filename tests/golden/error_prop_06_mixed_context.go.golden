package main

import (
	"os"
	"strconv"
)
import "strconv"

func processData(path string) (int, error) {
	tmp, err := os.ReadFile(path)

	if err != nil {
		return 0, err
	}
	var data = tmp
	tmp1, err1 := strconv.Atoi(string(data))

	if err1 != nil {
		return 0, err1
	}
	return tmp1, nil
}
